spring:
  application:
    name: waito-consumer
  config:
    import:
      - optional:file:.env[.properties]
      - optional:file:../.env[.properties]
      - classpath:/common.yml
server:
  port: 8090
